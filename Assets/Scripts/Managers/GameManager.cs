using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    public const float mapSize = 4;

    // List to store colors that are already assigned to players
    public static List<Color> allPlayersColors = new List<Color>();
}
